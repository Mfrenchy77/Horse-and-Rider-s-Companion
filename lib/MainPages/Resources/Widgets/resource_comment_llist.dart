import 'package:flutter/material.dart';

class ResourceCommentList extends StatelessWidget {
  const ResourceCommentList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Resource Comment List');
  }
}
