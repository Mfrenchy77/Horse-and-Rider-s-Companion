name: database_repository
description: Dart package which manages the database domain.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  cache:
    path: ../cache
  cloud_firestore: ^5.1.0
  equatable: ^2.0.3
  firebase_auth: ^5.1.2
  firebase_auth_platform_interface: ^7.2.1 
  firebase_core: ^3.2.0
  firebase_core_platform_interface: ^5.0.0
  firebase_remote_config: ^5.0.3
  firebase_storage: ^12.1.1
  flutter:
    sdk: flutter 
  html: ^0.15.0
  http: ^1.1.2  
  image_picker: ^1.0.4
dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  plugin_platform_interface: ^2.1.3
  very_good_analysis: ^7.0.0
